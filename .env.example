# Server port (default: 3001)
# PORT=3001

# Amp home directory — where thread JSON files are stored (default: ~)
# AMP_HOME=

# Path to the amp CLI binary (default: auto-detected from PATH)
# AMP_BIN=
