// Default context window for Claude models (Sonnet 4/Opus 4)
export const DEFAULT_MAX_CONTEXT_TOKENS = 168000;

// Maximum number of images that can be attached to a single message
export const MAX_ATTACHED_IMAGES = 5;

// Maximum file size for image attachments (10MB)
export const MAX_IMAGE_SIZE_BYTES = 10 * 1024 * 1024;
